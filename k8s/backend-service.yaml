apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: taskplatform
spec:
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP
  type: ClusterIP
